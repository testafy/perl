WWW::Testify is a perl module that can be installed in the standard way:

  perl Makefile.PL
  make
  make test
  make install

If you checked this code out directly from github you'll also need to install
Module::Install.

